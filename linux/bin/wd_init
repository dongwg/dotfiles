#!/bin/sh

# get vundle.vim if it doesn't exist
if [ ! -d ~/.vim/bundle/Vundle.vim ]; then
  git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim
fi
  

# Copy vimrc
cp ~/dotfiles/linux/etc/vimrc ~/.vimrc




